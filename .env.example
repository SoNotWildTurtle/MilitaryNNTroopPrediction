# Copy this file to .env and edit values for your local environment.
# Safe defaults keep the app usable with placeholder imagery and local MongoDB.

DATA_DIR=data
MONGO_URI=mongodb://localhost:27017
DB_NAME=troop_db

# Optional: required only for live Sentinel Hub imagery.
SENTINEL_CLIENT_ID=
SENTINEL_CLIENT_SECRET=
SENTINEL_INSTANCE_ID=
